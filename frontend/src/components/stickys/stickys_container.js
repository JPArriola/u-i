import { connect } from 'react-redux';
import Stickys from './stickys';

const mSTP = (state) => {
  return {

  };
};

const mDTP = (dispatch) => {
  return {

  };
};

export default connect(mSTP, mDTP)(Stickys);