import { connect } from 'react-redux';
import Albums from './albums';

const mSTP = (state) => {
  return {

  };
};

const mDTP = (dispatch) => {
  return {

  };
};

export default connect(mSTP, mDTP)(Albums);